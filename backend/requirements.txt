Django>=5.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.0
# psycopg 3 (avec pool) : Django l'utilise en priorité s'il est installé.
# Le pool garde des connexions ouvertes -> pages rapides meme apres une pause
# (plus de "reveil" de Supabase a chaque requete).
psycopg[binary,pool]>=3.2
python-dotenv>=1.0
